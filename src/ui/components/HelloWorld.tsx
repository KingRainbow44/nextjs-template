// This makes the component render on the client,
// also allowing it to use React client-side features.
"use client";

function HelloWorld() {
    return (
        <span>Hello world!</span>
    );
}

export default HelloWorld;
